---
description: Run the Re:ZERO Loop
argument-hint: "<task>"
---

If the user request is `init`, load and follow `${CLAUDE_PLUGIN_ROOT}/skills/rezero-init/SKILL.md`.
If the user request is `bgm <on|off|true|false>`, load and follow `${CLAUDE_PLUGIN_ROOT}/skills/rezero/SKILL.md` for BGM configuration only.
Otherwise load and follow `${CLAUDE_PLUGIN_ROOT}/skills/rezero/SKILL.md`.

User request:
$ARGUMENTS
