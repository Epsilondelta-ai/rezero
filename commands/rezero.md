---
description: Start the Re:ZERO autonomous agent loop for implementing user stories with death regression
---

Execute the Re:ZERO Loop agent directive. Read and follow the instructions in `$CLAUDE_PLUGIN_ROOT/prompt.md` to autonomously implement user stories from `task.json`.

1. Read `$CLAUDE_PLUGIN_ROOT/prompt.md` for the full agent directive.
2. Follow every step described in the directive exactly.
3. Use `task.json` in the current working directory for user stories.
4. Use `progress.txt` in the current working directory for accumulated knowledge.
5. Use `rem.md` in the current working directory for technical debt tracking.

Begin the Re:ZERO Loop now.
